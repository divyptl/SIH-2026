# Intentionally minimal. Do not build this out until Phases A-G
# (baseline inference, wrapper, schema integration, preprocessing,
# tests, evaluation) are working and evaluated — see README.md,
# "Roadmap". Premature LoRA/domain-adaptation code here adds
# complexity before there's a baseline to compare against.
